# 变更记录

本文件记录仓库迭代中具有代表性的改动，详细任务和验收条件见 [项目迭代计划](docs/iteration-plan.md)。

## 未发布

### 文档

- 补充算法、基础语法、并发、网络、gRPC、RPC 与企业服务入口的目录说明。
- 升级根 README 的二级目录导航，并标注外部服务依赖。

### 企业应用骨架

- 新增仅管理员可访问的审计记录分页查询接口，为后续 MySQL 审计持久化保留读取抽象。
- MySQL 仓储新增审计记录持久化与自动迁移，集成测试覆盖审计写入和读取。
- 新增当前用户资料接口、用户与任务列表分页、任务归属隔离和稳定业务错误码。
- 增加可选的 Redis 用户资料缓存、RabbitMQ 任务事件与依赖健康状态；默认保持关闭。
- 使用 bcrypt 和 HS256 JWT 升级认证，增加管理员角色控制与内存审计记录。
- 增加 OpenAPI 接口契约及 MySQL、Redis、RabbitMQ 本地 Docker Compose 编排。
- 增加可选 MySQL 仓储集成测试和 GitHub Actions 完整检查工作流。

## 2026-07-08

### 第一、二阶段

- 修正观察者模式目录命名为 `designpattern/observer`。
- 完善 Gin 常用示例、文档和路由测试。
- 统一 gRPC 端口说明，补充检查脚本的 `go vet`。
- 为网络、并发、运行时、加密等基础示例及独立子模块补充测试。
